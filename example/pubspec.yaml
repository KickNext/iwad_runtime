name: iwad_runtime_example
description: "KickNext example app for the iwad_runtime plugin."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  iwad_runtime:
    path: ../
  path_provider: ^2.1.5
  web: ^1.1.1
  window_manager: ^0.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
