# Run with `dart run ffigen --config ffigen.yaml`.
name: IwadRuntimeBindings
description: |
  Bindings for `src/iwadr.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/iwad_runtime_bindings_generated.dart'
headers:
  entry-points:
    - 'src/iwadr.h'
  include-directives:
    - 'src/iwadr.h'
comments:
  style: any
  length: full
